import { RegistroConformidad } from './registro-conformidad';

describe('RegistroConformidad', () => {
  it('should create an instance', () => {
    expect(new RegistroConformidad()).toBeTruthy();
  });
});
